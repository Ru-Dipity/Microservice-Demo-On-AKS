{{ $svc := index .Values.services "session-db" }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: session-db
  namespace: {{ include "sock-shop.namespace" . }}
  labels:
    name: session-db
    {{- include "sock-shop.labels" . | nindent 4 }}
spec:
  replicas: {{ $svc.replicas | default 1 }}
  selector:
    matchLabels:
      name: session-db
  template:
    metadata:
      labels:
        name: session-db
      annotations:
        prometheus.io/scrape: "false"
    spec:
      containers:
        - name: session-db
          image: {{ $svc.image }}
          args: {{ toYaml $svc.args | nindent 12 }}
          imagePullPolicy: {{ $.Values.imagePullPolicy }}
          ports:
            - name: redis
              containerPort: {{ $svc.port }}
          {{- if $svc.securityContext }}
          securityContext:
            {{- toYaml $svc.securityContext | nindent 12 }}
          {{- end }}
          volumeMounts:
            - mountPath: /tmp
              name: tmp-volume
            - mountPath: /data
              name: tmp-volume
      volumes:
        - name: tmp-volume
          emptyDir:
            medium: Memory
      nodeSelector:
        {{- toYaml $.Values.nodeSelector | nindent 8 }}
---
apiVersion: v1
kind: Service
metadata:
  name: session-db
  labels:
    name: session-db
    {{- include "sock-shop.labels" . | nindent 4 }}
  namespace: {{ include "sock-shop.namespace" . }}
spec:
  ports:
    - port: {{ $svc.port }}
      targetPort: {{ $svc.port }}
  selector:
    name: session-db
